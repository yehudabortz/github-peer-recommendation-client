import React from "react";
import "../css/Loading.css";
import "../css/TextClasses.css";

const Loading = () => {
  return (
    <div className={"loading-wrap {"}>
      <h2 className={"muted-text"}>Loading...</h2>
    </div>
  );
};

export default Loading;
